if git diff --cached --name-status | grep -qE '^[AM][[:space:]]+\.env(\.test)?$'; then
	echo "BLOCKED: .env or .env.test is staged for commit. Run: git reset HEAD .env .env.test"
	exit 1
fi

npx lint-staged
