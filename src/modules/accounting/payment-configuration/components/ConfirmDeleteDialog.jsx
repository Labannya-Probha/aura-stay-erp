import { AlertTriangle } from 'lucide-react'
export default function ConfirmDeleteDialog({open,terminal,isDeleting=false,onCancel,onConfirm}){if(!open||!terminal)return null;return <div className="fixed inset-0 z-[110] grid place-items-center bg-slate-950/40 p-4"><button className="absolute inset-0" aria-label="Cancel" onClick={isDeleting?undefined:onCancel}/><div role="alertdialog" className="relative z-10 w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="grid h-11 w-11 place-items-center rounded-xl bg-red-50 text-red-600"><AlertTriangle className="h-5 w-5"/></div><h2 className="mt-4 text-lg font-semibold">Delete payment terminal?</h2><p className="mt-2 text-sm leading-6 text-slate-600"><strong>{terminal.name||terminal.terminal_name}</strong> will be removed. This cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onCancel} disabled={isDeleting} className="rounded-xl border px-4 py-2 text-sm font-semibold">Cancel</button><button onClick={onConfirm} disabled={isDeleting} className="rounded-xl bg-red-600 px-4 py-2 text-sm font-semibold text-white">{isDeleting?'Deleting…':'Delete terminal'}</button></div></div></div>}
