import RestaurantTableGrid from '../components/RestaurantTableGrid.jsx'

export default function TableViewTab() {
  return <RestaurantTableGrid title="Table view" subtitle="25 tables are available here. Blocked tables reopen their saved draft bill." />
}
